spring.application.name=api

spring.datasource.url=jdbc:postgresql://localhost:5432/ecopoints_db
spring.datasource.username=postgres
spring.datasource.password=Wez9129@

spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
 
# JWT secret (DO NOT commit a real secret)
# Leave empty here and provide the secret via environment variable or JVM/system property when running.
ecopoints.jwt.secret=